// this is sample code to make sure everything is working
const greetUser = (user: string) => {
  console.log(`\nGreetings, ${user}!`);
  console.log(`\nThank you for choosing this repo as your jumpstart kit.`)
  console.log(`\nIf you are seeing this message, your environment is ready for Typescript development\n\n`)
}

greetUser(`user of _ts_basic_template`);
